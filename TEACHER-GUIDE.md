# دليل المدرس: إزاي تضيف محتوى من غير برمجة

> **قاعدة ذهبية:** غيّر النص اللي بين علامتي الاقتباس " " بس. ما تمسحش علامات الاقتباس ولا الفواصل `,` ولا الأقواس `{ } [ ]`.
> لو غلطت، المنصة بتعرض رسالة حمرا فيها اسم الملف والسطر التقريبي. ارجع للنسخة القديمة وحاول تاني.
> مفيش حاجة بتتكسر: أسوأ حاجة إن الدرس ده يعرض رسالة ودّية لحد ما تصلحه.

## 1) غيّر بياناتك (`data/config.json`)
اسمك، رقم الواتساب (أرقام بس بالصيغة الدولية بدون +: `201012345678`)، رابط تليجرام، والمواعيد.

## 2) أضف شرح درس
1. انسخ ملف `data/lessons/a-t1-u1-l1.json` (أو `b-t1-u1-l1.json`) وسمّي النسخة **بنفس رقم الدرس**.
   أرقام الدروس موجودة في `data/courses.json` (مثال: `a-t1-u2-l3` = كورس أ، ترم 1، وحدة 2، درس 3).
2. غيّر `"id"` جوّه الملف لنفس الرقم.
3. عدّل: `hook` (جملة تفتكرها)، `objectives`، `keyPoints`، `glossary`، `timeline`، `mistakes`.
4. بعد ما تراجع كل معلومة بنفسك، احذف السطرين `"draft": true` و `"needs_teacher_review": true` أو خلّيهم `false`.

## 3) أضف فيديو
حط كود الفيديو (11 حرف، الجزء بعد `v=` في رابط يوتيوب) هنا: `"video": { "id": "dQw4w9WgXcQ" }`. خلّي الفيديو **Unlisted**.

## 4) أضف أسئلة كويز
```json
{
  "q": "نص السؤال؟",
  "options": ["اختيار 1", "اختيار 2", "اختيار 3", "اختيار 4"],
  "answer": 1,
  "explain": "سبب الإجابة الصح."
}
```
`answer` هو ترتيب الإجابة الصح **بيبدأ من صفر** (الأول = 0، التاني = 1 ...).

## 5) تابع اللي ناقص
افتح `الموقع/#/checklist` تلاقي جدول بكل الدروس وإيه اللي ناقص فيها (محتوى، فيديو، أسئلة، مراجعة).

## 6) الترم التاني (تانية بكالوريا)
لما تجهز أسماء الدروس من الكتاب، ابعتها وتتضاف في `data/courses.json` (غيّر `"status": "coming_soon"` إلى `"active"`).
